# Test configuration
ktor {
    development = true
    deployment {
        port = 8080
    }
}

database {
    host = "localhost"
    port = 5432
    name = "ai_assistant_test"
    user = "ai_user"
    password = "test_password"
}
